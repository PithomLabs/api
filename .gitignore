# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Ignore the .env files which contain important infos
*.env.*
.env
*.build

# But don't ignore .env.example, which is an example .env file
!.env.example

tmp/
.vscode

# Ignore binaries
bin/
